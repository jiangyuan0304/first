create table student ( Sno char(7) primary key, Sname char(7) not null, Ssex char(2), Sage tinyint, Sdept char(20));


create table SC();create table student ( Sno char(7) primary key, Sname char(7) not null, Ssex char(2), Sage tinyint, Sdept char(20));

constraint 约束；局促；态度不自然
literature n. 文学文献；文艺；著作

create table SC(Sno char(7) not null, Cno char(6) not null, Grade smallint, primary key(Sno, Cno), foreign key(Sno) references student(Sno), foreign key(Cno) references Course(Cno));


## 插入数据
insert into student values(11112, "wjm", "w", 21, "economics");

insert into SC(Sno, Cno, Grade) values("11111", "c03", 78);
# 可以只填充两列数据

# 查询经过计算后的列
select Sname , 2021-Sage from student;

# 消除重复行
distinct

# 比较
>; < ;>= ; 
# 确定范围
between;and; not between and
# 确定集合
in； not in
# 字符匹配
like; not like
# 空值
is null ; is not null
# 多重条件
and ;or  


表名.列名  是为了去除显示重复的列。简明需要的列的信息
select * from a表名 join b表名 on 联合的条件

# 当为表指定了别名之后，查询的语句的所有地方都要使用别名